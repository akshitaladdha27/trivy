[extend]
useDefault = true

[[rules]]
id = "detect-passwords"
description = "Detect generic password assignments"
regex = '''(?i)(password|passwd|pwd)\s*[:=]\s*["'][^"']{6,}["']'''
tags = ["password"]

